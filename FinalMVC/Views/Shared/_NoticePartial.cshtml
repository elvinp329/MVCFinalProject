@using FinalMVC.DAL.Entities;
@model List<Notice>

@foreach (var item in Model)
{
    <div class="single-notice-left mb-23 pb-20">
        <h4>@item.PostDate</h4>
        <p>@item.Text </p>
    </div>
}